<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Memory Settings
    |--------------------------------------------------------------------------
    */
    'memory' => [
        'limit' => '256M', // حد الذاكرة
        'cache_ttl' => 3600, // وقت تخزين الكاش (بالثواني)
    ],

    /*
    |--------------------------------------------------------------------------
    | Cache Settings
    |--------------------------------------------------------------------------
    */
    'cache' => [
        'enable_query_cache' => true, // تفعيل تخزين الاستعلامات
        'query_cache_time' => 3600,   // وقت تخزين الاستعلامات
        'enable_route_cache' => true,  // تفعيل تخزين المسارات
        'enable_config_cache' => true, // تفعيل تخزين الإعدادات
    ],

    /*
    |--------------------------------------------------------------------------
    | Database Settings
    |--------------------------------------------------------------------------
    */
    'database' => [
        'chunk_size' => 1000,         // حجم المجموعة للعمليات الكبيرة
        'max_execution_time' => 600,   // أقصى وقت للتنفيذ (بالثواني)
    ],

    /*
    |--------------------------------------------------------------------------
    | Session Settings
    |--------------------------------------------------------------------------
    */
    'session' => [
        'driver' => 'file',           // نوع تخزين الجلسات
        'lifetime' => 120,            // عمر الجلسة (بالدقائق)
        'expire_on_close' => false,   // إنهاء الجلسة عند إغلاق المتصفح
    ],
];
